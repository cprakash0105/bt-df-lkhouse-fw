# Business glossary cleared — terms grow organically via KC (Dataplex).
# Synonym matching runs against KC terms loaded at runtime via CatalogReader.
# Do not add terms here; approve them through the discovery flow instead.
business_glossary: []
