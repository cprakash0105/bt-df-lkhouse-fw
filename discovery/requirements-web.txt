# Semantic Discovery Web — Lean deps (profiling decoupled to service)
pyyaml>=6.0
numpy>=1.24.0
fastapi>=0.104.0
uvicorn>=0.24.0

# GCP (for approval handler + GCS + Firestore cache)
google-cloud-storage>=2.10.0
google-cloud-dataplex>=2.0.0
google-cloud-firestore>=2.13.0
google-auth>=2.20.0

# RAG (ChromaDB — lightweight vector store)
chromadb>=0.4.0
